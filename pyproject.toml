[project]
name = "smartfeedback-ai"
version = "0.1.0"
description = "Turn customer voice feedback into actionable business intelligence."
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.setuptools]
packages = ["app", "app.agents"]
